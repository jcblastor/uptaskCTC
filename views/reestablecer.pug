doctype html
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1= nombrePagina
      p Agrega tu correo electrónico para reestablecer la contraseña

      if locals.messages
        .alertas
          each categoria in Object.keys(locals.messages)
            each error in messages[categoria]
              .alerta(class=`${categoria}`)
                p= error
                
      form.caja-login(method="POST" action="/reestablecer")
        .campo
          label(for="email") E-mail
          input(type="email" name="email" placeholder="Ingresa tu Email" value= email)
  
        .campo.enviar
          input.boton(type="submit" value="Reestablecer contraseña")

        .campo.acciones
          a(href="/iniciar-sesion") Ya lo recordé, volcer a Iniciar sesión